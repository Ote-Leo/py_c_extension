#ifndef SAMPLE_HEADER
#define SAMPLE_HEADER

/**
 * This header isn't used for anything,
 * but it's an example for how the include_dirs option can be used to setup.py
 * to build extension with multiple header directories.
*/

static const char* SOME_STRING = "sample_string";

#endif  // SAMPLE_HEADER